name: Data Quality Auto-Fixer

on:
  schedule:
    # Runs at 01:00 UTC (04:00 Turkey Time) every day
    - cron: '0 1 * * *'
  workflow_dispatch: # Allows manual triggering from GitHub UI

jobs:
  autofix:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'
        
    - name: Install System Dependencies (Chrome & Xvfb)
      run: |
        sudo apt-get update
        sudo apt-get install -y google-chrome-stable xvfb
        
    - name: Install Python Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run Data Quality Auto-Fixer
      env:
        DATABASE_URL: ${{ secrets.DATABASE_URL }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        ZENROWS_API_KEY: ${{ secrets.ZENROWS_API_KEY }}  # Optional if needed for scraper logic
      run: |
        xvfb-run --auto-servernum --server-args="-screen 0 1920x1080x24" python data_quality_autofix.py
